/** Inert Client face; generated quota Remote types are consumed by the UI. */
export const inject = [] as const

/** Client plugin body: quota behavior lives in the Host service and the UI half. */
export function apply(): void {
}
